def run_public_festival_sync(sync_log, page_size=100):
	# 공공데이터 API 호출
	# FestivalPublic 저장
	return {
			"insert": insert_count,
			"update": update_count,
			"error": error_count,
			"total": total_count,
	}